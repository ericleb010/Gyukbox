export enum Event {
    CONNECT = 'connect',
    DISCONNECT = 'disconnect',
}

export enum Action {
    JOIN = 'join',
    ADD_SONG = 'addSong',
    PLAY_SONG = 'play',
    CHAT_LIST = 'chatList',
    ADD_CHAT_MESSAGE = 'addChatMsg',
}
